<html isEditable="true"><head isEditable="true"></head><body isEditable="true">{% for item in items %}
<div class="card" isEditable="true">
    <div class="card-header" isEditable="true">
        <h2 class="card-title" isEditable="true">{{ item.title }}</h2>
    </div>
    <div class="card-body" isEditable="true">
        <p class="card-description" isEditable="true">{{ item.description }}</p>
        <div class="card-footer" isEditable="true">
            <button class="btn" isEditable="true">Acción</button>
        </div>
    </div>
</div>
{% endfor %}</body></html>