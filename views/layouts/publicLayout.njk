<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>{{ title }}</title>

    <script src="/js/libraries/jquery.min.js"></script>

    <link
      rel="stylesheet"
      href="/css/styles.css"
    />
    <script
      type="module"
      src="/js/main.js"
      defer
    ></script>
  </head>

  <body>
    {% include 'components/common/Navbar/index.njk' %} {% include
    'components/common/Sidebar/index.njk' %}
    <div
      id="layoutContainer"
      class="container"
    >
    
      <div
        id="contentMain"
        class="contentMain"
      >
        {# Bloque donde se renderizará el contenido específico de cada ruta #}
        {% block content %}{% endblock %}
      </div>
    </div>
  </body>
  <script>
    // Pasar el token a una variable de JavaScript
    const token = '{{ token }}'; // Asegúrate de escapar correctamente
    const currentPath = '{{ currentPath }}';
    const isAutenticated = '{{ isAuthenticated }}'; // Asegúrate de escapar correctamente

    // console.log('Token desde Nunjucks:', token);
    // console.log('Autenticado desde Nunjucks:', isAutenticated);
    // console.log('Ruta actual:', currentPath);
  </script>
</html>
